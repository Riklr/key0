# Network: "testnet" (Base Sepolia) or "mainnet" (Base)
KEY2A_NETWORK=testnet

# Your receive-only wallet address (0x-prefixed, 40 hex chars)
KEY2A_WALLET_ADDRESS=0xYourWalletAddressHere

# Secret for signing access tokens (minimum 32 characters)
KEY2A_ACCESS_TOKEN_SECRET=change-me-to-a-random-string-at-least-32-chars

# Optional: override the default RPC URL
# KEY2A_RPC_URL=https://sepolia.base.org

# Server port
PORT=3001
